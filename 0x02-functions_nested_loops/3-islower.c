#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 * _islower - main block
 * Description: function that checks for lowercase character.
 * Return: 0
 */
int _islower(int c)
{
if (islower(c))
return (1);
else
{
return (0);
}
}